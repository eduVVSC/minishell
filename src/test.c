#include "../include/minishell.h"

/* int main(void)
{
	char **command;

	command[0] = "/bin/cd";
	command[1] = "execute";
	command[2] = NULL;

	execve(command[0], command, NULL);

	return(0);
} */

/*  int main(void)
{
	char **command;

	command[0] = "/bin/echo";
	command[1] = "-n";
	command[2] = "ola";
	command[3] = "ola";
	command[4] = NULL;

	execve(command[0], command, NULL);

	return(0);
} */
